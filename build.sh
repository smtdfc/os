#!/bin/bash
set -e

echo "[*] Copying build artifacts..."
mkdir -p build
cp linux/arch/x86/boot/bzImage build/
cp rootfs.cpio.gz build/
cp linux/.config build/kernel.config
echo "[✓] Done!"#!/bin/bash
set -e

echo "[*] Copying build artifacts..."
mkdir -p build
cp linux/arch/x86/boot/bzImage build/
cp rootfs.cpio.gz build/
cp linux/.config build/kernel.config
echo "[✓] Done!"#!/bin/bash
set -e

echo "[*] Copying build artifacts..."
mkdir -p build
cp linux/arch/x86/boot/bzImage build/
cp rootfs.cpio.gz build/
cp linux/.config build/kernel.config
echo "[✓] Done!"#!/bin/bash
set -e

echo "[*] Copying build artifacts..."
mkdir -p build
cp linux/arch/x86/boot/bzImage build/
cp rootfs.cpio.gz build/
cp linux/.config build/kernel.config
echo "[✓] Done!"#!/bin/bash
set -e

echo "[*] Copying build artifacts..."
mkdir -p build
cp linux/arch/x86/boot/bzImage build/
cp rootfs.cpio.gz build/
cp linux/.config build/kernel.config
echo "[✓] Done!"#!/bin/bash
set -e

echo "[*] Copying build artifacts..."
mkdir -p build
cp linux/arch/x86/boot/bzImage build/
cp rootfs.cpio.gz build/
cp linux/.config build/kernel.config
echo "[✓] Done!"#!/bin/bash
set -e

echo "[*] Copying build artifacts..."
mkdir -p build
cp linux/arch/x86/boot/bzImage build/
cp rootfs.cpio.gz build/
cp linux/.config build/kernel.config
echo "[✓] Done!"#!/bin/bash
set -e

echo "[*] Copying build artifacts..."
mkdir -p build
cp linux/arch/x86/boot/bzImage build/
cp rootfs.cpio.gz build/
cp linux/.config build/kernel.config
echo "[✓] Done!"
